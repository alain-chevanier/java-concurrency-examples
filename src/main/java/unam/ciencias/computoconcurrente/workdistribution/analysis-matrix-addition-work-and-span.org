#+title Analysis of Work and Span for Matrix Addition

* Calculating Work
Let ~A_1(n)~  el trabajo necesario para sumar dos matrices cuadradas de tamaño `nxn`:

#+begin_src latex
A_1(n)  = 4*A_1(n/2) + O(1)
        = 4*(4*A_1(n/4)+O(1)) + O(1)
        = 4*4*A_1(n/4) + 4*O(1) + O(1)
        = 4^2*(4*A_1(n/8) + O(1)) + 4*O(1)* + O(1)
        = 4^3*A_1(n/8) + 4^2*O(1) + 4*O(1) + O(1)
        ... and so on
        ... then we hace the following
        = 4^(log (n)) + O(1)*(4^log(n) + ... + 4^2 + 4 + 1)
        = [2^log(n)]^2 + O(1)*(4^[log(n) + 1] - 1) / (4 - 1)
        = O(n^2) + O((4 * n^2 - 1)/3)
        = O(n^2)
#+end_src

*Notes*:
1. The sum ~1 + a + a^2 + ... + a^k = (a^[k+1] - 1) / (a-1).~
2. By definition ~2^log(n) = n.~

* Proof by Induction
To be proved ~A_1(n)~ es ~O(n^2)~.

** Base case ~n = 1, 2~

#+begin_src latex
A_1(1) es 1
A_1(2)  = 4*A_1(1) + O(1)
        = 4 + O(1)
        = 2^2 + O(1)
#+end_src

** Inductive step

Let's suppor that it hold for all ~k < n~, that is ~A_1(k)~ is ~O(k^2)~, i.e  ~A_1(k) = a*k^2 + b~ for some integers ~a!=0~ and ~b~.

#+begin_src
A_1(n)  = 4 A1(n/2) + O(1)
        ... por hipotesis de induccion
        = 4*(a*(n/2)^2 + b) + O(1)
        = 4*a*(n^2)/4 + 4*b + O(1)
        = a*n^2 + [4*b + O(1)]
        = O(n^2) + O(1)
        = O(n^2)
#+end_src
